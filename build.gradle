apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "PartyPlanning"
version = '1.0'
mainClassName = "App"

run{
  standardInput = System.in
}

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
